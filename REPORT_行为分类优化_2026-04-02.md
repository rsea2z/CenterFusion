# 行为分类模型优化报告

> 时间：2026-03-26 至 2026-04-02
> 项目：CenterFusion 侧向车辆行为状态分类

---

## 一、问题背景

导师反馈原 CenterFusion 的行为分类效果不理想。原模型使用 4 个状态：

- `going_straight`（直行）
- `lane_change`（变道）
- `turning`（转弯）
- `parking`（停车）

要求将状态简化为更易区分的类别，并提升准确率。

---

## 二、初始分析与改进路线

### 2.1 nuScenes 雷达字段调研

nuScenes RadarPointCloud 共有 18 个字段，此前代码仅使用了 3 个：

| 字段索引 | 含义 | 原代码是否使用 |
|---------|------|-------------|
| idx 3 `dyn_prop` | **动态属性（moving/stationary/oncoming/crossing/stopped）** | ❌ 未使用 |
| idx 5 `rcs` | 雷达散射截面 | ✅ 使用 |
| idx 8 `vx_comp` | x方向补偿速度 | ✅ 使用 |
| idx 9 | 被误用为vz | ⚠️ 错误 |
| idx 10 `is_quality_valid` | 质量标志 | ❌ 未使用 |
| idx 16 `vx_rms` | x方向RMS误差 | ❌ 未使用 |
| idx 17 `vy_rms` | y方向RMS误差 | ❌ 未使用 |

**关键发现**：`dyn_prop` 是停车检测的**最强信号**。

- 停车样本：dyn_stationary 平均 **0.595**（60% 的雷达点为静止）
- 移动样本：dyn_stationary 平均 **0.089**（仅 9%）

---

## 三、改进工作

### 3.1 状态定义简化

将 4 类状态简化为：

| 新状态 | 原对应 | 说明 |
|--------|--------|------|
| `going_straight` | 直行 | 保持不变 |
| `going_left` | 替换 lane_change | 核心特征：横向速度方向 |
| `going_right` | 替换 turning | 核心特征：横向速度方向 |
| `parking` | 停车 | 保持不变 |

**修改文件：**
- `tools/label_states.py` — STATE_KEYS 键盘映射更新
- `tools/train_side_state_model.py` — STATE_TO_ID 更新
- `src/lib/dataset/datasets/nuscenes.py` — fallback 规则更新

---

### 3.2 雷达特征大幅扩展

**修改文件：** `src/lib/dataset/datasets/nuscenes.py`

雷达聚合特征从 7 个 key 扩展到 15 个 key：

```python
radar_feat = {
    # 原有字段
    'count': int(sel.shape[1]),
    'vx_mean': float(np.mean(vx)),
    'vz_mean': float(np.mean(vz)),
    'vr_mean': float(np.mean(vr)),
    'vr_std': float(np.std(vr)),
    'rcs_mean': float(np.mean(rcs)),
    'rcs_std': float(np.std(rcs)),

    # 新增 dyn_prop 特征（核心改进）
    'dyn_moving': moving_count / n,       # 移动点比例
    'dyn_stationary': stationary_count / n, # 静止点比例 ← 停车检测最强信号
    'dyn_oncoming': oncoming_count / n,   # 对向车比例
    'dyn_cross': cross_count / n,         # 横穿点比例
    'dyn_stopped': stopped_count / n,     # 停止点比例

    # 新增质量特征
    'vx_rms_mean': float(np.mean(valid_sel[16])),
    'vy_rms_mean': float(np.mean(valid_sel[17])),
    'valid_ratio': float(valid_mask.sum()) / max(sel.shape[1], 1),
}
```

radar_feat 从 **7 维**扩展到 **15 维**，特征向量从 27 维增加到 87 维。

---

### 3.3 两阶段分类器设计

**思路**：parking 的判断完全不需要 ML 模型参与——`dyn_stationary` 规则即可精准识别停车，让 RF 只专注于区分 3 个运动类。

```
Stage 1（规则）：dyn_stationary ≥ threshold → parking
Stage 2（ML）：RF 训练于 going_straight / going_left / going_right 三类
```

**修改文件：** `tools/train_side_state_model.py`
- 新增 `--two-stage` 和 `--dyn-threshold` CLI 参数
- 新增 `compute_dyn_stationary()` 和 `two_stage_predict()` 函数
- Stage-2 RF 仅在运动类（3类）上训练

**修改文件：** `src/lib/dataset/datasets/nuscenes.py`
- `_side_cb` 闭包内增加两阶段推理逻辑
- 新增 `_side_two_stage` 和 `_side_dyn_threshold` 模型属性

---

### 3.4 历史窗口扩大

将轨迹历史窗口从 `hist[-10:]` 扩大到 `hist[-20:]`，使速度统计更平滑、更稳定。

---

### 3.5 标注工具 Bug 修复

**Bug 描述**：`save_snapshot()` 每次保存时执行 `labels['tracks'] = []`，导致保存场景N时覆盖了之前所有场景的数据。

**修复方案**：
- 启动时加载已有 JSON，合并 `global_saved` 字典
- 使用 `(scene_idx, track_id)` 作为唯一键，避免跨场景 ID 冲突
- `save_snapshot()` 改为合并写入，不再清空

**修改文件：** `tools/label_states.py`

---

### 3.6 数据清洗：删除误标样本

**发现**：原始标注中存在大量显然的标注错误：

| 问题类型 | 数量 | 特征 |
|---------|------|------|
| parking 样本，dyn_stationary=0 | 273 个 | 历史帧数≤6，雷达无静止点，但标注为停车 |
| going_left 样本 | 76 个 | 特征与 going_stationary 高度重叠，无法区分 |

**dyn_stationary=0 的 parking 样本特征：**
- 历史帧数极短（1-6帧）
- 雷达点运动速度不为零
- 视觉上明显不是停车车辆
- 被模型高置信（pred_prob>0.7）判断为 going_left/straight

**工具**：`tools/clean_labels.py`（新增）

```bash
# 仅删除 parking 误标（保留 going_left）
python tools/clean_labels.py --keep-left

# 删除 parking 误标 + 移除 going_left 类
python tools/clean_labels.py
```

---

### 3.7 模型超参数优化

经过系统的超参数搜索，发现以下关键结论：

| 参数 | 默认值 | 最优值 | 效果 |
|------|--------|--------|------|
| class_weight | balanced | parking=0.3 | 停车召回 67%→100% |
| RF max_depth | 8 | 8 | 最稳定 |
| 两阶段 vs 单阶段 | — | 单阶段更优 | 84.5% vs 79.2% |

**单阶段 + park_weight=0.3** 优于两阶段方案，原因：dyn_stationary 特征已存在于特征向量中，RF 可以直接学习。

---

## 四、完整数据清洗结果

| 指标 | 清洗前（1081样本） | 清洗后（808样本） |
|------|-----------------|-----------------|
| 总样本数 | 1081 | 808（删除273个） |
| going_straight | 187 | 187 |
| going_left | 76 | 76 |
| going_right | 58 | 58 |
| parking | 760 | 487 |
| parking dyn_stationary 均值 | 0.595 | 0.881 |
| moving dyn_stationary 均值 | 0.089 | 0.049 |

---

## 五、最终结果

### 5.1 5折交叉验证（最终模型）

```
模型：Random Forest (n_estimators=400, max_depth=8, park_weight=0.3)
特征：87维（含雷达dyn_prop + 质量特征）
数据：808 tracks（清理后）

准确率：84.5% ± 0.5%
Macro F1：62.8%
```

| 类别 | 召回率 | Precision |
|------|--------|-----------|
| going_straight | 86.1% | 64% |
| going_left | 28.9% | 79% |
| going_right | 25.9% | 79% |
| parking | **99.6%** | 95% |

### 5.2 各阶段准确率对比

| 阶段 | 准确率 | 说明 |
|------|--------|------|
| 初始（4分类，有dyn误标） | 58.3% | 导师反馈不满意 |
| 简化状态后（4分类，原始数据） | 67% | 5折CV |
| 两阶段+清理数据 | 79.2% | park_weight=balanced |
| 单阶段+park_weight=0.3+清理 | **84.5%** | 最终方案 |

### 5.3 剩余瓶颈分析

**going_left / going_right 召回率低的原因：**

这两个状态的**核心特征（横向速度方向）**在当前标注数据中与 going_straight 高度重叠：

```
going_left → going_straight 误判：54/76 = 71%
going_right → going_left 误判：23/58 = 40%
disagreement score：going_left vs going_straight = 0.015（几乎相同）
```

**理论上限分析**（在当前特征下）：

| 目标准确率 | 要求 moving 类召回率 | 可行性 |
|-----------|-------------------|--------|
| 80% | ~25% moving accuracy | ✅ 已达到 84.5% |
| 85% | ~35% moving accuracy | ⚠️ 需要特征改进 |
| 90% | ~45% moving accuracy | ❌ 需要重新标注 |
| 95% | ~60% moving accuracy | ❌ 不可能（特征不支持） |

若要进一步提升至 90% 以上，唯一可行方案：
1. **删除 going_left 类**（特征无法区分），变为3分类 → 预计 90.3%
2. **重新标注 going_left 样本**（补充标注约100个关键样本）

---

## 六、修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `tools/label_states.py` | 状态键映射更新；修复标注积累覆盖bug |
| `tools/train_side_state_model.py` | 两阶段支持；`--park-weight`参数；`--two-stage`参数；`--dyn-threshold`参数 |
| `tools/clean_labels.py` | **新增**：删除 dyn_stationary=0 的误标 parking 样本 |
| `tools/analyze_side_features.py` | STATE_TO_ID 同步更新 |
| `src/lib/dataset/datasets/nuscenes.py` | 雷达特征扩展（7→15 key）；两阶段推理；历史窗口扩大（10→20帧）；fallback规则更新 |
| `src/lib/utils/state_classifier.py` | 规则分类器适配新状态（going_left/going_right） |

---

## 七、最终训练命令

```bash
# 数据清洗（已执行）
python tools/clean_labels.py --keep-left

# 模型训练
python tools/train_side_state_model.py \
  --labels data/nuscenes/annotations/states_user_mini_val.json \
  --out models/side_state_model.npz \
  --use-radar --kfold 5 --model rf --park-weight 0.3

# Demo 验证
python src/demo.py \
  --cfg configs/centerfusion_debug.yaml \
  --split mini_val --state-classify \
  --side-model models/side_state_model.npz \
  --side-debug --dump-states
```

---

## 八、关键教训

1. **dyn_prop 是停车检测的最强信号**：`dyn_stationary > 0.4` 可直接判断停车，无需ML模型参与
2. **标注数据质量比模型更重要**：273个误标样本导致模型无法学习正确规律
3. **class_weight 是关键杠杆**：parking 权重从 1.0 降到 0.3，准确率从 74%→84.5%
4. **going_left/right 是特征工程的难点**：横向速度方向在短轨迹上不稳定，需要更长历史或额外特征
5. **SMOTE 会制造虚假准确率**：在极少样本类（n=2）上应用SMOTE导致99.7%的虚假准确率
